# >>> xui network deploy local HTTP proxy >>>
# 本机服务（当 NAT_HTTP_LISTEN 暴露到回环以外时，亦含本机 docker 容器）可用此 HTTP 代理出站，
# 它把流量转发到 NAT SSH 动态 SOCKS 隧道。监听地址与 SOCKS 目标由 deploy 渲染填入。
listen-address __NAT_HTTP_LISTEN__
forward-socks5t / __NAT_SOCKS_LISTEN__ .
# <<< xui network deploy local HTTP proxy <<<
